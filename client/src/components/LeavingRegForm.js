import React from 'react';
import { Formik, Field, Form, ErrorMessage } from 'formik';
import { Button, Modal, Grid, Typography, Checkbox, TextField, FormControlLabel } from '@material-ui/core';

const emailRegexPattern = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i;

export const LeaveRegForm = () => (
  <div>
    <Button>Hello</Button>
  </div>
)